(ns abc.wiki)

;; While WikiData seems better overall, the recall/quality for Japanese is quite low.
;; Evaluate if recent dbpedia dumps (2021) are good enough/better. Start with queries on author styles/genres.
;; http://ja.dbpedia.org/