# Ortho-Detect Phase 2.5 — ML k-Fold Cross-Validation

Date: 2026-07-05
Gold set: `data/ortho-gold/sentences-llm-300.jsonl` (n=300, LLM-labeled, accept=213, reject=87)
k = 5 (strided fold assignment; deterministic — the sampler pre-shuffled the gold file, so no separate shuffle is needed; each record appears in exactly one test fold)
Model: `linfa_logistic::LogisticRegression` (char-only features via `extract_char_features` + `features_to_vector`; same train config as `ab-ortho-detect-ml` train: `max_iterations=500`, label encoding `1=accept / 0=reject` as `i32`).

## Per-fold results (held-out test fold)

| fold | recall | precision | f1 | accuracy | n_test |
|---:|---:|---:|---:|---:|---:|
| 1 | 0.9556 | 0.9556 | 0.9556 | 0.9333 | 60 |
| 2 | 0.9111 | 0.9762 | 0.9425 | 0.9167 | 60 |
| 3 | 1.0000 | 1.0000 | 1.0000 | 1.0000 | 60 |
| 4 | 0.9750 | 0.9512 | 0.9630 | 0.9500 | 60 |
| 5 | 0.9535 | 0.9535 | 0.9535 | 0.9333 | 60 |

| metric | mean | min | max |
|---|---:|---:|---:|
| recall | 0.9590 | 0.9111 | 1.0000 |
| precision | 0.9673 | — | — |
| f1 | 0.9629 | — | — |
| accuracy | 0.9467 | — | — |

## Method note (Phase 2 Task 6 fix applied)

Evaluation uses `Predict::predict`, which returns the actual class labels directly. **Do NOT** use `predict_probabilities >= 0.5`: linfa-logistic's `label_classes` designates the *more-frequent* class as positive, so thresholded probabilities invert on this data (correct impl shows ~0.9x recall; a buggy probabilities-threshold impl shows ~0.05 recall — the inversion signature). Labels are `Array1<i32>` (`1=accept, 0=reject`); linfa-logistic requires `Ord` labels, ruling out `f64`.

## Honest generalization vs train-accuracy ceiling

- Train-accuracy recall on the full 300 LLM labels (Phase 2.5 Task 5/6 trainer, no hold-out): **0.958** — this is a *ceiling*, not generalization.
- Hold-out mean recall (5-fold CV, this report): **0.9590** (min 0.9111, max 1.0000).

The ~0 percentage-point gap between the ceiling and the hold-out mean is the train/test optimism on this feature set.

## Phase 2.5 Task 7 gate

Promote `--ortho-detect ml` from EXPERIMENTAL to stable **only if mean recall >= 0.85**.

- Mean recall = **0.9590**
- Gate (>= 0.85): **PASS**

