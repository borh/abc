#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "${BASH_SOURCE[0]}")/.."

system="$(nix eval --impure --raw --expr builtins.currentSystem)"
exec nix build --no-link ".#checks.${system}.clj-kondo"
